## 接手

算法团队报过来一句：

> 新集群的 NCCL 测试跑通了，正确性检查也过了，**但 busbw 只有 190 GB/s**，我们预期是 400。是不是网络没配好？

已知的硬件配置：

- 2 个节点，每节点 8 卡 GPU
- 每节点 **8 张 400G RoCE 网卡**（一卡一 rail）
- 测试用 `all_reduce_perf`，共 16 个 rank

先自己算一遍理论峰值再往下看：

```
理论峰值 busbw = 网卡数 × 单口带宽 ÷ 8
              = 8 × 400 Gbps ÷ 8
              = 400 GB/s
```

实测 190 GB/s，只有 **47.5%**。参考判定标准：≥90% 优秀，80–90% 可接受，**低于 80% 属于异常**。
所以这确实是个需要查的问题，而且"只有一半"这个比例本身就是很强的线索。

<Callout type="tip" title="怎么玩">
在终端里按目标一步步查。输入 <code>goals</code> 看目标，<code>hint</code> 要提示。
四个目标达成即过关。

思路提示：先看结果、再看 NCCL 到底选了哪条路、再看硬件实际状态、最后看配置。
</Callout>

<Terminal
  id="slow-allreduce-quest"
  host="root@k8s-work-105"
  intro={`2 节点 × 8 卡，每节点 8 × 400G RoCE。理论 busbw 400 GB/s，实测 190 GB/s。
测试任务已跑完，launcher pod 日志还在。`}
  commands={[
    {
      cmd: 'kubectl get pods',
      output: `NAME                     READY   STATUS      RESTARTS   AGE
nccl-test-launcher-0     0/1     Completed   0          6m
nccl-test-node-0-0       1/1     Running     0          7m
nccl-test-node-1-0       1/1     Running     0          7m`,
    },
    {
      cmd: 'kubectl logs nccl-test-launcher-0 --tail=12',
      goal: '确认实测 busbw 与理论峰值的差距',
      hint: 'launcher 的日志末尾就是 all_reduce_perf 的结果表',
      aliases: ['kubectl logs nccl-test-launcher-0 | tail -12'],
      output: `#      size    count    type   redop    time   algbw   busbw #wrong
#       (B)   (elem)                          (us)  (GB/s)  (GB/s)
    67108864 16777216   float     sum   712.4   94.20  176.63      0
   134217728 33554432   float     sum  1398.1   96.00  180.00      0
   268435456 67108864   float     sum  2761.2   97.22  182.29      0
   536870912 134217728  float     sum  5462.9   98.28  184.28      0
  1073741824 268435456  float     sum 10802.4   99.40  186.38      0
  2147483648 536870912  float     sum 21421.7  100.25  187.97      0
  4294967296 1073741824 float     sum 42638.9  100.73  188.87      0
  8589934592 2147483648 float     sum 84744.2  101.36  190.05      0
# Out of bounds values : 0 OK
# Wrong values : 0 OK
# Avg bus bandwidth : 183.308

# 正确性没问题（Wrong values 0），纯粹是带宽不达标。
# 峰值 190.05 GB/s ÷ 400 GB/s ≈ 47.5%，几乎正好是一半。`,
    },
    {
      cmd: 'kubectl logs nccl-test-launcher-0 | grep NET',
      goal: '确认 NCCL 实际选用的网络通道与网卡数量',
      hint: 'NCCL 初始化时会打印它选中的网络插件和 HCA 列表，这是判断走没走 RDMA 的第一手证据',
      aliases: ['kubectl logs nccl-test-launcher-0 | grep -i "NET/IB"'],
      output: `nccl-test-node-0-0:41:52 [0] NCCL INFO NET/IB : Using [0]mlx5_0:1/RoCE [1]mlx5_1:1/RoCE [2]mlx5_2:1/RoCE [3]mlx5_3:1/RoCE
nccl-test-node-0-0:41:52 [0] NCCL INFO NET/IB : GPU Direct RDMA Enabled for HCA 0 'mlx5_0'
nccl-test-node-1-0:39:50 [0] NCCL INFO NET/IB : Using [0]mlx5_0:1/RoCE [1]mlx5_1:1/RoCE [2]mlx5_2:1/RoCE [3]mlx5_3:1/RoCE
nccl-test-node-1-0:39:50 [0] NCCL INFO Using network IB

# 好消息：走的是 NET/IB（RDMA），不是 NET/Socket（TCP），GPUDirect 也开了。
# 坏消息：列表里只有 mlx5_0 到 mlx5_3 —— 只用了 4 张卡。`,
    },
    {
      cmd: 'kubectl exec nccl-test-node-0-0 -- ibdev2netdev',
      goal: '核对节点上实际存在的 RDMA 设备与链路状态',
      hint: '机器上到底有几张 RDMA 卡？它们都 Up 了吗？',
      aliases: ['kubectl exec nccl-test-node-0-0 -- ibdev2netdev -v'],
      output: `mlx5_0 port 1 ==> ens1f0np0 (Up)
mlx5_1 port 1 ==> ens1f1np0 (Up)
mlx5_2 port 1 ==> ens2f0np0 (Up)
mlx5_3 port 1 ==> ens2f1np0 (Up)
mlx5_4 port 1 ==> ens3f0np0 (Up)
mlx5_5 port 1 ==> ens3f1np0 (Up)
mlx5_6 port 1 ==> ens4f0np0 (Up)
mlx5_7 port 1 ==> ens4f1np0 (Up)

# 8 张卡全都在，全都 Up。mlx5_4 到 mlx5_7 完全没被 NCCL 用上。`,
    },
    {
      cmd: 'kubectl exec nccl-test-node-0-0 -- ibstat mlx5_4',
      output: `CA 'mlx5_4'
	CA type: MT4129
	Number of ports: 1
	Firmware version: 28.42.1000
	Port 1:
		State: Active
		Physical state: LinkUp
		Rate: 400
		Link layer: Ethernet

# mlx5_4 状态完全正常：Active / LinkUp / 400G。
# 排除硬件与链路问题 —— 是软件没用它。`,
    },
    {
      cmd: 'kubectl exec nccl-test-node-0-0 -- env | grep NCCL',
      goal: '找出配置错误的那一项',
      hint: '既然卡都在、链路都好，就该看 NCCL 的选卡参数了',
      aliases: ['kubectl exec nccl-test-node-0-0 -- printenv | grep NCCL'],
      output: `NCCL_ALGO=Ring
NCCL_DEBUG=INFO
NCCL_DEBUG_SUBSYS=INIT,NET,GRAPH
NCCL_IB_DISABLE=0
NCCL_IB_HCA=mlx5_0,mlx5_1,mlx5_2,mlx5_3
NCCL_IB_QPS_PER_CONNECTION=8
NCCL_IB_TC=106
NCCL_SOCKET_IFNAME=bond1
NCCL_CROSS_NIC=1

# 找到了：NCCL_IB_HCA 只列了 4 张卡。
# 这个值大概是从 4 卡机型的配置直接抄过来的。`,
    },
    {
      cmd: 'kubectl exec nccl-test-node-0-0 -- nvidia-smi topo -m',
      output: `        GPU0 GPU1 GPU2 GPU3 GPU4 GPU5 GPU6 GPU7 NIC0 NIC1 NIC2 NIC3 NIC4 NIC5 NIC6 NIC7
GPU0     X   NV18 NV18 NV18 NV18 NV18 NV18 NV18  PIX  NODE NODE NODE SYS  SYS  SYS  SYS
GPU4    NV18 NV18 NV18 NV18  X   NV18 NV18 NV18  SYS  SYS  SYS  SYS  PIX  NODE NODE NODE

Legend: PIX = 同一 PCIe switch 内，NODE = 同 NUMA 跨 switch，SYS = 跨 NUMA

# 重点：GPU4-GPU7 的近邻网卡是 NIC4-NIC7（PIX），
# 而这四张卡恰好就是被 NCCL_IB_HCA 排除掉的那四张。
# 结果 GPU4-7 的流量只能跨 NUMA 去借 NIC0-3，既少了一半带宽又多了跨 NUMA 开销。`,
    },
    {
      cmd: 'kubectl exec nccl-test-node-0-0 -- show_gids',
      output: `DEV     PORT  INDEX  GID                                      IPv4          VER  DEV
mlx5_0  1     3      0000:0000:0000:0000:0000:ffff:0a68:1467  10.104.20.103 v2   ens1f0np0
mlx5_4  1     3      0000:0000:0000:0000:0000:ffff:0a68:1477  10.104.20.119 v2   ens3f0np0

# v2 条目都在 index 3，GID 配置没问题。`,
    },
    {
      cmd: 'kubectl describe pod nccl-test-node-0-0 | grep -A3 Limits',
      output: `    Limits:
      nvidia.com/gpu:  8
      rdma/hca:        1
      memory:          512Gi

# rdma/hca 资源已申请（共享设备插件模式，1 个即可访问全部 HCA）。`,
    },
    {
      cmd: 'kubectl exec nccl-test-node-0-0 -- ethtool -S ens3f0np0 | grep -i pause',
      output: `     rx_pause_ctrl_phy: 0
     tx_pause_ctrl_phy: 0
     rx_prio3_pause: 0
     tx_prio3_pause: 0

# 这张卡压根没跑过流量，PFC 计数全 0 —— 又一个它没被用上的证据。`,
    },
  ]}
/>

## 结论应该长什么样

| 证据 | 说明了什么 |
| --- | --- |
| `Wrong values : 0 OK`，busbw 190 GB/s | 功能正常，纯粹是带宽问题 |
| 190 / 400 ≈ 47.5%，接近整整一半 | 强烈暗示"资源用了一半" |
| NCCL 日志 `NET/IB`、GPUDirect Enabled | **走的是 RDMA，不是回退 TCP** |
| NCCL 日志只列出 `mlx5_0`–`mlx5_3` | 只有 4 张卡参与 |
| `ibdev2netdev` 显示 8 张卡全部 Up | 硬件与链路都正常 |
| `ibstat mlx5_4`：Active / LinkUp / 400 | 未使用的卡本身完全健康 |
| `NCCL_IB_HCA=mlx5_0,mlx5_1,mlx5_2,mlx5_3` | **根因** |
| `nvidia-smi topo -m` | GPU4–7 的近邻卡正好被排除，还额外跨了 NUMA |

**根因**：`NCCL_IB_HCA` 只列了 4 张网卡（很可能是从 4 卡机型的配置抄来的），
另外 4 张 400G 卡完全闲置。可用网络带宽只有一半，所以 busbw 也差不多是一半。

**修法**：

```bash
# 方案一：前缀匹配，把所有 mlx5 设备都纳入
NCCL_IB_HCA=mlx5

# 方案二：显式列全 8 张（想精确控制时用）
NCCL_IB_HCA=mlx5_0,mlx5_1,mlx5_2,mlx5_3,mlx5_4,mlx5_5,mlx5_6,mlx5_7
```

改完重跑，期望 busbw 到 360 GB/s 以上（理论峰值的 90%）。

<Callout type="warn" title="为什么「跑通了」不等于「配对了」">
这次故障里正确性检查全过、NCCL 也确实走的 RDMA，唯一的症状就是数字偏低。
如果没有人去算理论峰值并对比，这套集群会带着一半的带宽一直服役下去。

所以 GPU 集群交付必须有一条硬性验收：**算出理论峰值，实测 busbw 达到 90% 才算通过。**
这个数字是唯一能一次性覆盖网卡、链路、拓扑、拥塞控制和参数配置的端到端指标。
</Callout>

<Callout type="note" title="顺便记住这几个「一半」的常见原因">
busbw 大约只有理论值一半时，嫌疑名单（按出现频率排）：

1. **`NCCL_IB_HCA` 少列了卡** —— 本例
2. **PCIe 槽位喂不满网卡** —— 400G 卡插在 Gen4 x16 上，上限只有约 250 Gb。
   用 `lspci -vv | grep LnkSta` 核对，详见 [PCIe：机内的那张网](/learn/l2-hpc/pcie-topology)
3. **端口速率协商降级**（400G 口协商成 200G）—— 用 `ibstat` 看 `Rate`
4. **NVLink 掉了几条链路** —— 机内变慢也会拉低整体 busbw，用 `nvidia-smi topo -m` 看 `NV#` 是否一致
5. **rail 布线接错** —— 同 rail 流量被迫上 spine（拓扑那节细讲）
6. **只有单向流量在跑** —— 测试参数或拓扑不对称

如果掉到只剩几分之一甚至十分之一，那通常不是"一半"类问题，而是**回退到 TCP 了**：
去日志里找 `NET/Socket`。
</Callout>

## 检查点

<Quiz
  id="qar-1"
  question="NCCL 日志里出现 NET/Socket 而不是 NET/IB，意味着什么？"
  options={[
    { text: 'NCCL 没有走 RDMA，回退到了 TCP，带宽会差一个量级', correct: true },
    { text: 'NCCL 用的是 InfiniBand 而不是 RoCE', feedback: '两者在日志里都显示为 NET/IB，区别在后面标注的 IB 还是 RoCE。' },
    { text: '只是日志格式不同，不影响性能', feedback: '这两条路径的性能差距非常大，绝不能忽略。' },
    { text: 'GPUDirect 没有启用', feedback: 'GPUDirect 有独立的日志行，与选哪个网络插件是两件事。' },
  ]}
  explain={<>这是判断「到底走没走 RDMA」最快的方法。常见原因：NCCL_IB_DISABLE=1、NCCL_IB_HCA 写错设备名、Pod 里看不到 RDMA 设备（没申请 rdma/hca）。功能都正常，只是慢十倍。</>}
/>

<Quiz
  id="qar-2"
  question="8 卡 × 400G 的节点，实测 busbw 190 GB/s。下面哪个排查顺序最合理？"
  options={[
    { text: '先算理论峰值 → 看 NCCL 选了哪条网络与几张卡 → 核对硬件链路 → 检查环境变量', correct: true },
    { text: '先抓包分析 RoCE 流量', feedback: 'RDMA 数据流抓不到内容，而且这一步应该在前面几层都排除后才做。' },
    { text: '先调 PFC/ECN 水位', feedback: '在确认「卡都用上了」之前调拥塞控制，是在解一个还没确诊的问题。' },
    { text: '先换网线', feedback: '链路状态用 ibstat 一看就知道，不需要盲换硬件。' },
  ]}
  explain={<>顺序的价值在于每一步都在排除一大块：算峰值确定有没有问题，看 NCCL 日志确定走没走 RDMA 和用了几张卡，核硬件排除物理层，最后才看配置。跳步骤就会变成盲猜。</>}
/>

<Quiz
  id="qar-3"
  question="本例中 nvidia-smi topo -m 提供了什么额外信息？"
  options={[
    { text: '被排除的四张卡正好是 GPU4–7 的近邻卡，导致这些 GPU 还要跨 NUMA 借用网卡', correct: true },
    { text: '证明 GPU 之间的 NVLink 有故障', feedback: '日志里 NVLink 显示为 NV18，是正常的。' },
    { text: '说明网卡固件版本不匹配', feedback: 'topo 命令不显示固件信息。' },
    { text: '说明 GPU 算力不足', feedback: 'topo 只反映拓扑关系，与算力无关。' },
  ]}
  explain={<>这条命令解释了「为什么不只是掉一半」：GPU4-7 失去了 PIX 近邻卡，被迫跨 NUMA（SYS）走 NIC0-3，既挤占带宽又增加延迟。拓扑亲和性是高性能网络里必须核对的一项。</>}
/>

## 这一关的落点

- GPU 集群的网络验收指标就一个：**busbw ≥ 理论峰值 90%**
- 理论峰值 = 网卡数 × 单口带宽 ÷ 8
- 判断走没走 RDMA：日志里找 `NET/IB` 还是 `NET/Socket`
- busbw 约为一半时，头号嫌疑是 `NCCL_IB_HCA` 少列了卡，其次是速率降级、rail 接错
- busbw 只剩几分之一时，去查是不是回退了 TCP
- `nvidia-smi topo -m` 要看 GPU 与 NIC 的近邻关系（PIX / NODE / SYS），跨 NUMA 会额外掉性能
- "跑通了"和"配对了"是两件事，没有基线对比就发现不了这类问题
